C++23 solutions to the `Advent of Code 2023`.

Input is read from the standard input, solution is on the standard output.

No special theme, just trying some C++23 features.

With `nix` can build and run the tests using:

    nix build 'github:xffox/adventofcode#adventofcode2023'
